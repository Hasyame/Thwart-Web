# Install as /etc/systemd/system/thwart-api.service.d/bgg.conf.
# Permit the BGG relay's outgoing HTTPS and DNS. The API listener remains
# 127.0.0.1:8787; all other service hardening stays in the main unit.
[Service]
IPAddressAllow=
IPAddressDeny=
